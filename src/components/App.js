
import React from 'react';

// Convert App component to a functional component
const App = () => {
    return (
        <div>
            <p>Now I can render any React component on any DOM node I want using ReactDOM.render</p>
        </div>
    );
};

// Ensure App is the default export
export default App;
